import { Component } from '@angular/core';

@Component({
  selector: 'skills-settings',
  template: require('./skillsSettings.component.html'),
  // styles: [require('./skillsSettings.component.scss')],
})
export class SkillsSettingsComponent {
}
